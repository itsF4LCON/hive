name: sensor

on:
  push:
    branches: [main]
    paths: ["sensor/**", ".github/workflows/sensor.yml"]
    tags: ["v*"]
  pull_request:
    paths: ["sensor/**"]

jobs:
  build:
    strategy:
      matrix:
        include:
          - runner: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
          - runner: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
    runs-on: ${{ matrix.runner }}
    defaults:
      run:
        working-directory: sensor
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --locked
      - run: cargo build --release --locked
      # Built on 22.04 so the binary only needs glibc 2.35 and runs on Ubuntu 22.04 and newer.
      - run: |
          objdump -T target/release/hive-sensor | grep -o 'GLIBC_[0-9.]*' | sort -Vu | tail -1
          cp target/release/hive-sensor hive-sensor-${{ matrix.target }}
      - uses: actions/upload-artifact@v7
        with:
          name: hive-sensor-${{ matrix.target }}
          path: sensor/hive-sensor-${{ matrix.target }}

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          merge-multiple: true
      - run: sha256sum hive-sensor-* > SHA256SUMS
      - uses: softprops/action-gh-release@v3
        with:
          files: |
            hive-sensor-*
            SHA256SUMS
